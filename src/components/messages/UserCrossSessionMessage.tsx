/** messages/UserCrossSessionMessage — feature-gated module (reconstructed) */
import * as React from 'react'

export function UserCrossSessionMessage(_props: {
  addMargin: boolean
  param: any
}): React.ReactElement {
  return null as any
}
